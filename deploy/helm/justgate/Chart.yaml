apiVersion: v2
name: justgate
description: justgate – multi-tenant observability proxy with admin UI
type: application
version: 1.1.7
appVersion: "1.1.7"

dependencies:
  - name: postgresql
    version: "16.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
