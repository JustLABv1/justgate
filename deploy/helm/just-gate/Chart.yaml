apiVersion: v2
name: just-gate
description: just-gate – multi-tenant observability proxy with admin UI
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: postgresql
    version: "16.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
